#include "main.h"
/**
 * mul - multiplies two integers
 * @c: the first number to multiply
 * @i: the second number to multiply whit the first number
 * Return: the result of mutiplication between c param and i param
 */
int mul(int c, int i)
{
	return (c * i);
}
